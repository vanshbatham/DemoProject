git init                              # 📁 Initialize git (if not already)
git remote add origin https://github.com/vanshbatham/DemoProject.git  # ⬅️ Set your GitHub repo
git add .                             # ➕ Add all files (WAR, Dockerfile, JSPs, etc.)
git commit -m "Initial commit with Dockerfile and Signup.war"  # 📝 Commit
git branch -M main                    # 🌿 Rename branch to main (if needed)
git push -u origin main               # 🚀 Push to GitHub
